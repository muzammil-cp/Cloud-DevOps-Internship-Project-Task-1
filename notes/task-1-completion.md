# Task 1 Completion Notes

## Status
**Completed**

## Work Completed
1. Ubuntu Linux development environment verified.
2. Hostname and user identity verified.
3. Git, curl, tree, and Python versions verified.
4. Git global identity and default branch configured.
5. SSH key authentication with GitHub tested successfully.
6. Project directory structure created.
7. README and `.gitignore` prepared.
8. Bash system-information script created and executed.
9. Bash setup automation script created and executed.
10. Network interface and IP configuration checked.
11. Internet connectivity tested with ping.
12. DNS resolution tested with nslookup.
13. HTTP headers tested with curl.
14. Network route tested with traceroute.
15. Listening ports inspected with `ss -tuln`.
16. Git branches, feature work, merge, push, and conflict resolution practiced.

## Important Git Work
A branch named `feature` existed initially. Therefore, creating
`feature/documentation` directly produced a Git reference namespace error.
The existing branch was renamed to `feature-old`, after which
`feature/documentation` was created successfully.

The documentation branch was pushed and merged into `main`.

A README merge conflict was also practiced and resolved. The final repository
was pushed to GitHub and verified with a clean working tree.

## Evidence
Screenshots supplied for the internship are stored in `screenshots/`.
