# Configuration

This directory stores safe, non-secret configuration used by the project.

## Files

- `environment.example` — example environment variables without real secrets.
- `git-config.txt` — Git identity and default branch settings used for the practical.

## Security Rule

Do not store passwords, private SSH keys, GitHub tokens, API keys, or other secrets in this directory.
